header 
  h1 Providence Startups
section#content
  ol
    each row in dbRows
      li
        article
          p
            b
              a.name(href='#{row.url}') #{row.name}
            |  - 
            #{row.description}
            - if(row.hiring)
              div.hiring 
                a(href='#{row.hiringUrl}') Hiring
